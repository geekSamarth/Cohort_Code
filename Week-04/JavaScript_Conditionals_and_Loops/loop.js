//  to perform looping on array element

let teas = ["masala", "ginger", "oolang", "orange", "lemon", "rose"];

console.log(teas.length); // calculating the length of the teas array

for (let h = 0; h < teas.length; h++) {
  console.log(`Fruit at index ${h} is: ${teas[h]}`);
}
